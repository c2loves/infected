# 3proxy minimal — NO AUTH, open proxy (USE WITH CAUTION)
nscache 65536

# Không yêu cầu auth
auth none

# HTTP proxy on port 8080
proxy -p8080

# Optional: enable basic logging (ghi file log trong cùng thư mục)
log 3proxy.log D
